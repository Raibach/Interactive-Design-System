fastapi==0.104.1
uvicorn[standard]==0.24.0
psycopg2-binary==2.9.9
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv>=1.0.1,<2.0.0
requests>=2.31.0
psutil>=5.9.0
pymilvus>=2.6.0
tenacity==8.2.3
sentry-sdk[fastapi]==2.3.1
openai>=1.0.0
# Password verification for /api/auth/login. The `users` table has held real
# bcrypt hashes ($2b$12$) since it was written; nothing could CHECK one until
# now, because no auth endpoint existed and this library was never installed.
bcrypt>=4.1.0,<6.0.0

# The local vector store (embedded milvus + in-process embeddings). Installed 2026-09-19
# with the Zilliz retirement; weights for BAAI/bge-small-en are cached locally, so no
# network is needed at first use.
milvus-lite>=2.5.0
sentence-transformers>=3.0.0
