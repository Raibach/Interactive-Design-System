Errors being hidden — the thing you're fighting
The run spinner can never appear. WritingAreaIndex.tsx:457-465 finds control-bar/canvas-footer with document.querySelectorAll — but every element lives inside the renderer's shadow root, so the reach is a no-op by construction. The alternative path writes into a tree that Run itself swaps out. The only writer that survives the swap is the one that cannot land.
"A fresh composer starts clean" silently doesn't happen. WritingAreaIndex.tsx:3131-3134 — document.querySelector('chat-panel')?.clearThread?.(). Shadow DOM again: never found, never cleared, no error. deepFind exists precisely for this and wasn't used.
Save silently drops the column widths. WritingAreaIndex.tsx:1515-1526 waits 100 ms for a column-widths-response that nothing in the repo dispatches; widths are always undefined and fall out of the payload as a missing field.
A control that can never fire. The layout emits third-column-toggle (workspace-layout.ts:782), the shell listens for toggle-third-column (WritingAreaIndex.tsx:4350). Real event, no listener; real listener, no sender. Plus save-requested — listened for, and no sender exists anywhere.
Six catalogued tags render as empty boxes because no element defines them: agent-card, filter-pill, search-bar, prompt-section, output-panel, version-trace. A seventh, role-dropdown, is defined but never imported, so it can never exist at runtime. The renderer fails loud for one case; the rest are silent.
State written and never read (dead mirrors of live facts): middleOpen, usageTotal — the shell accumulates the seat's spend into a total that no footer path can reach — and _promptSessions, which is set three times and read never.
Deleting a card can run twice. card-delete has two live hosts — WritingAreaIndex.tsx:3686-3690 and ConsolePage.tsx:73-91 — each calling the delete and re-implementing the model filter. One confirm, two writers.
My own addition has the disease. The chat flush I wrote this session swallows failure with .catch(() => 0) and logs only on success (WritingAreaIndex.tsx:1637-1643). Same class as the rest; flagging it against myself.
Fallbacks and second homes
Sections live in four places — the elements, the model path, currentPromptSession.leftColumnContent, and repairSectionsRef. One sync path (1941-1974) can re-apply an older serialized copy over newer text after a tab change.
The canvas tree is composed by React, not the envelope. setOutputColumn (916-1047) authors AgentCanvas/AgentFlow/OutputControls/CanvasFooter by hand. The backend never emits those names — zero hits — so that whole chain exists only because React writes it. That's why editing the canvas feels different from editing everything else.
Hardcoded React chrome where the model supplies only data: the decision dialog (4477-4508), the failure pane (4687-4740), the exit dialog (4511-4523).
Your direct question: is the canvas Lit-catalog compliant?
Yes. agent-canvas, agent-flow, canvas-footer, output-controls, compiled-output-viewer are each fully cataloged (Pascal + kebab), in the tag registry, imported in main.tsx, and in the renderer's map — the same chain as every other component. Children come from the envelope; the elements declare slots. Zero eval, zero innerHTML in any component source. Three honest deviations: two documented nestings (output-controls and compiled-output-viewer instantiate <model-selector-button> inside their own templates; ConsoleCardGrid renders <agent-card-element>), the catalog and registry still advertise a "seat" slot that agent-canvas no longer renders (and the playground page still relies on it), and four of the five have no Figma-map entry.

AGENTIC_EDITOR and the dead code
The folder is not awful code — it is not code at all: 13 markdown files, 16 images, one JSON, zero source files. Nothing imports it (22 references, all comments), it's gitignored, and its own README says what it is — a working record, with "NOT SEEN" and "hand-fed mock" stated plainly. The cost is only the dead citations pointing into it.

The truly dead React cluster, unreachable from main.tsx: PromptDashboardCanvas, FlipCard, FeaturedCard, StorybookDocCard, TeacherChat, MemoriesTab, the React TraceFeed, VersionHistoryPanel, ResizableSplitter, ChatMessage.ts, tabMessagesData.ts, and MobileLayout (a stub that returns null, imported but never rendered). FeaturedCard still carries data-tag="featured-card" — the exact tag commit 39765ca deleted from the registry, in a file that came back from the dead and whose only consumer is dead too. The shell also still listens for four events whose senders were these retired components.

The #007 correction
#007 was real — annotation-prose:chat-navigation-bar — closed 2026-09-12 when its check stopped deriving it, then retired from the register. The corrections ledger cited it as if it were still registered, which is the "number that resolves to nothing" your own rule calls worse than no number. I corrected the text to cite only #021 and to record what happened to #007. Audit still GREEN, both catalogs.

Nothing else was touched. When you want to start killing silences, the first three I'd fix are the run spinner, the thread clear, and the toggle-third-column name mismatch — each is small, and each is a failure the app currently hides from you.